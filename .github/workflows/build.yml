name: CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the app
        run: ./gradlew build
      - name: Make gradlew executable
        run: chmod +x ./gradlew
        
